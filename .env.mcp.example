# Master Toggle Agent MCP Configuration
# Copy this to .env.mcp and fill in your credentials

# Required: Supabase MCP credentials
SUPABASE_PROJECT_REF=cxzllzyxwpyptfretryc
SUPABASE_ACCESS_TOKEN=your-personal-access-token-or-service-role-key

# Optional: Configuration overrides
SUPABASE_ROLE=service_role  # or 'anon' for limited access
MCP_READ_ONLY=false         # Set to true for read-only mode
NODE_ENV=development        # development, staging, or production
MASTER_TOGGLE_PORT=8080     # WebSocket server port

# Agent Repository Database (if different from main project)
# AGENT_REPO_PROJECT_REF=texxwmlroefdisgxpszc
# AGENT_REPO_ACCESS_TOKEN=your-agent-repo-token